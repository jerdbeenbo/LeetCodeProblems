/*
 * @lc app=leetcode id=118 lang=rust
 *
 * [118] Pascal's Triangle
 */

// @lc code=start
impl Solution {
    pub fn generate(num_rows: i32) -> Vec<Vec<i32>> {
        
        //initialise return vector
        let mut rVec: Vec<Vec<i32>> = Vec::new();

        //num_rows = size of return vector
        for i in 0..num_rows ( // -> loop amount of num_rows
            //value of index will be how many items in row
            
        )
    }
}
// @lc code=end

